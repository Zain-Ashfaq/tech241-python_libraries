# Libraries

# Libraries are collections of functions (pre-built code) that do a job for you
# We can import these libraries to make our own code easier to understand, be less complex and faster write

from random import randint, random
import math


# print(randint(1,3))

print(random())

num_float = 23.1

print(math.ceil(num_float))
print(math.floor(num_float))
print(math.pi)